def filter(name: str) -> str:
    """Generate standard filter component ID."""
    return f"{name}-filter"


def worksheet(name: str) -> str:
    """Generate standard worksheet component ID."""
    return f"{name}-worksheet"